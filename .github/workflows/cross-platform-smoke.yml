name: Cross-platform smoke

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    name: npx smoke test (${{ matrix.os }} / Node ${{ matrix.node }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: [20, 22, 24]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - name: Pack
        working-directory: packages/ctxpeek
        run: pnpm pack --pack-destination ../..

      - name: Smoke — bash
        if: runner.os != 'Windows'
        shell: bash
        run: |
          tarball=$(ls ctxpeek-*.tgz | head -n1)
          npm install -g "./$tarball"
          ctxpeek --version
          ctxpeek --help

      - name: Smoke — cmd /c (Windows footgun coverage)
        if: runner.os == 'Windows'
        shell: cmd
        run: |
          for /f "delims=" %%i in ('dir /b ctxpeek-*.tgz') do set TARBALL=%%i
          npm install -g .\%TARBALL%
          cmd /c npx -y ctxpeek --version
          cmd /c npx -y ctxpeek --help
